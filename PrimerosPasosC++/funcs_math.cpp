#include <iostream>
#include <cmath>

int main(){

    double x = 3;
    double y = 5;
    double z;

    //z = std::max(x, y);
    //z = std::min(x, y);
    //z = pow(x, y); //funcion exponecial
    //z = sqrt(9); //raiz cuadrada
    //z = abs(-3); //valor absoluto 
    //z = roud(3.14); //redondear para arriba  valores
    //z = ceil(3.14); //redondear para abajo  valores


    std::cout << z;

    return 0;
}